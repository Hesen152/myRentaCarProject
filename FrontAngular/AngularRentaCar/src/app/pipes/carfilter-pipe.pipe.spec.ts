import { CarfilterPipePipe } from './carfilter-pipe.pipe';

describe('CarfilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CarfilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
